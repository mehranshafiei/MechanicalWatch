pipeline {
    agent {
		docker { image 'mehranak/ubuntu_qt' }
	}
	
    stages {
        stage('Print environment variables') {
            steps {
				echo 'Checking Envronments'
            }
        }
    }
}
